{:paths ["src" "test"]
 :tasks {test {:doc "Run the cljc contract + behavioral test under babashka"
               :extra-paths ["test"]
               :requires ([kinaxis.main-test])
               :task (let [{:keys [fail error]} (clojure.test/run-tests 'kinaxis.main-test)]
                       (System/exit (if (pos? (+ fail error)) 1 0)))}}}
